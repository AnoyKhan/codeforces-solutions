# 🧩 Problem: B - Even Numbers

## ✅ Summary:
তোমাকে একটি পূর্ণসংখ্যা n ইনপুট দেওয়া হবে।
তুমি 2 থেকে n পর্যন্ত সব জোড় সংখ্যা (even numbers) প্রিন্ট করবে।
যদি n <= 1 হয়, তাহলে প্রিন্ট করবে -1।

---

## 📥 Input
- A single integer `n`  
  (1 ≤ n ≤ 1000)

---

## 📤 Output
- Print all even numbers from `2` to `n`, each on a new line.
- If there are no even numbers (i.e., `n <= 1`), print `-1`.

---

## 💡 Example 1
### Input:
```mermaid
8
```
### Output:
```
2
4
6
8
```
---
## 💡 Example 2
### Input:
```mermaid
1
```
### Output:
```mermaid
-1
```

---

## 🧠 Logic:
- লুপটি শুরু হবে i = 2 থেকে এবং প্রতি ধাপে i += 2 হবে।
- লুপটি i <= n পর্যন্ত চলবে এবং প্রতি ইটারেশনে i প্রিন্ট করবে।
- যদি n <= 1 হয়, তাহলে কোনো even সংখ্যা পাওয়া যাবে না, তাই সরাসরি -1 প্রিন্ট করা হবে।
---
## 💻 C++ Code
```cpp
#include <iostream>
using namespace std;
int main() {
    int n, i;
    cin >> n;
    for(i = 2; i <= n; i += 2) {
        cout << i << endl;
    }
    if(n <= 1) {
        cout << "-1";
    }
    return 0;
}
```
## ✅ Tags:
Loops, For Loop, Even Numbers, C++, Beginner

---
## 🔚 Final Notes:

এই প্রবলেমটি শেখায় কীভাবে নির্দিষ্ট step size (i += 2) ব্যবহার করে লুপ চালাতে হয়।
এছাড়া conditional output (-1) ব্যবহার করে exceptional case handle করাও শেখায়।
