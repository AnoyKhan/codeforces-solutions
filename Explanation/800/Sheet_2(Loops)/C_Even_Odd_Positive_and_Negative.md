# 🧩 Problem: C - Even, Odd, Positive and Negative

## ✅ Summary:
তোমাকে একটি পূর্ণসংখ্যা `n` ইনপুট দেওয়া হবে,  
তারপর `n`টি পূর্ণসংখ্যা ইনপুট নিতে হবে।  
এরপর গণনা করতে হবে কয়টি সংখ্যা **Even, Odd, Positive, Negative**,  
এবং সেই সংখ্যা গুলো আলাদা লাইনে প্রিন্ট করতে হবে।

---

## 📥 Input:
প্রথম লাইনে একটি পূর্ণসংখ্যা `n`,  
এরপর পরের লাইনে `n`টি সংখ্যা।

**Example Input:**
```
5
-5 0 -3 -4 12
```
---
## 📤 Output:
প্রতিটি শ্রেণির সংখ্যা আলাদা লাইনে প্রিন্ট করতে হবে।

**Example Output:**
```mermaid
Even: 2
Odd: 3
Positive: 1
Negative: 2
```

---

## 🧠 Logic:
- একটি `for` লুপ চালিয়ে প্রতিটি সংখ্যা ইনপুট নেওয়া হয়।
- যদি `j % 2 == 0` → `Even++`
- অন্যথায় → `Odd++`
- যদি `j > 0` → `Positive++`
- যদি `j < 0` → `Negative++`
- শেষে সব কাউন্টার প্রিন্ট করা হয়।

---

## 💡 Code:
```cpp
#include <iostream>
using namespace std;
int main() {
    int n, j, Even = 0, Odd = 0, Positive = 0, Negative = 0;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> j;
        if (j % 2 == 0) {
            Even++;
        } else {
            Odd++;
        }
        if (j > 0) {
            Positive++;
        } else if (j < 0) {
            Negative++;
        }
    }
    cout << "Even: " << Even << endl
         << "Odd: " << Odd << endl
         << "Positive: " << Positive << endl
         << "Negative: " << Negative;
    return 0;
}
```
---
## ✅ Tags:
Loops, Conditionals, Counting, C++, Beginner

---
## 🔚 Final Notes:

এই প্রবলেমটি লুপের ভিতরে multiple conditional checks ব্যবহারের প্র্যাকটিস করায়।
এছাড়া এটি সংখ্যা শ্রেণিবিন্যাসের ধারণা (even, odd, positive, negative) বোঝায়।