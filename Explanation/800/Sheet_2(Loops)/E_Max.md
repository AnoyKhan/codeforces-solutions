# 🧩 Problem: E - Max Number

## ✅ Summary:
তোমাকে একটি পূর্ণসংখ্যা `n` ইনপুট দেওয়া হবে,  
তারপর `n`টি সংখ্যা ইনপুট নিতে হবে।  
তুমি ইনপুট দেওয়া সংখ্যাগুলোর মধ্যে **সর্বোচ্চ সংখ্যা (maximum value)** বের করে প্রিন্ট করবে।

---

## 📥 Input:
প্রথম লাইনে থাকবে `n`,  
তারপর পরের লাইনে থাকবে `n`টি পূর্ণসংখ্যা।

**Example Input:**
```mermaid
5
1 7 3 9 5
```
---
## 📤 Output:
```mermaid
9
```
---

## 🧠 Logic:
- একটি variable `max` ইনিশিয়াল করা হয়েছে `0` দিয়ে।
- প্রতিবার ইনপুট নেওয়ার সময় `if (j >= max)` হলে `max = j` সেট করা হয়।
- লুপ শেষে `max` প্রিন্ট করলে সেটাই সর্বোচ্চ মান।

🧩 **Note:**  
যদি ইনপুটে সব সংখ্যা negative হয়, তাহলে `max` এর initial মান `0` হওয়ায় ভুল ফলাফল হতে পারে।  
সেক্ষেত্রে ভালো হয় যদি `max` কে প্রথম ইনপুটের মান দিয়েই initialize করা হয়।

---

## 💡 Code:
```cpp
#include <iostream>
using namespace std;
int main() {
    int n, j, max = 0;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> j;
        if (j >= max) {
            max = j;
        }
    }
    cout << max;
    return 0;
}
```
---
## ✅ Tags:
Loops, Conditionals, Maximum Value, C++, Beginner

---
## 🔚 Final Notes:
এই প্রবলেমটি লুপের ভিতরে তুলনা (comparison) করার ধারণা দেয়।
এটি ডেটা থেকে সর্বোচ্চ মান বের করার বেসিক ধাপ, যা পরে array, sorting, algorithm-এ অনেক কাজে লাগে।