# 🧩 Problem: G - Factorial

## ✅ Summary:
তোমাকে একটি পূর্ণসংখ্যা `n` ইনপুট দেওয়া হবে,  
যা জানাবে কয়টি সংখ্যা দেওয়া হবে।  
এরপর `n`টি সংখ্যা ইনপুট নিতে হবে,  
এবং প্রতিটি সংখ্যার **factorial** প্রিন্ট করতে হবে।

---

## 📥 Input:
প্রথম লাইনে একটি পূর্ণসংখ্যা `n`  
তারপর `n`টি সংখ্যা ইনপুট দেওয়া হবে।

**Example Input:**
```
3
5
3
6
```
---
## 📤 Output:
প্রতিটি ইনপুট সংখ্যার factorial নতুন লাইনে প্রিন্ট করতে হবে।

**Example Output:**
```mermaid
120
6
720
```

---

## 🧠 Logic:
- `n` বার ইনপুট নেওয়া হবে।
- প্রতিটি ইনপুটের জন্য একটি ভেরিয়েবল `t` ১ দিয়ে শুরু হবে।
- তারপর `for` লুপের মাধ্যমে `1` থেকে `j` পর্যন্ত multiply করে factorial বের করা হবে।
- প্রতিটি সংখ্যার factorial প্রিন্ট করার পর `t` কে আবার ১ এ reset করা হবে।

---
## 💡 Code:
```cpp
#include <iostream>
using namespace std;
int main() {
    long long n, j, t = 1;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> j;
        if (j > 0) {
            for (int k = 1; k <= j; k++) {
                t *= k;
            }
        }
        cout << t << endl;
        t = 1;
    }
    return 0;
}
```
---
## ✅ Tags:
Loops, Factorial, Nested Loop, C++, Beginner

---

## 🔚 Final Notes:
এই প্রবলেমটি nested loop ব্যবহার করে কীভাবে একাধিক সংখ্যার factorial বের করতে হয় তা শেখায়।
এটি iteration, multiplication, এবং variable reset-এর ধারণা পরিষ্কার করে।