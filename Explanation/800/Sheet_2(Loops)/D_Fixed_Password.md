# 🧩 Problem: D - Fixed Password

## ✅ Summary:
একটি প্রোগ্রাম লিখতে হবে যা বারবার ইউজারের কাছ থেকে একটি পূর্ণসংখ্যা ইনপুট নেবে।  
যদি ইনপুটটি **১৯৯৯** হয়, তাহলে প্রিন্ট করবে **"Correct"** এবং প্রোগ্রাম শেষ করবে।  
অন্যথায় প্রিন্ট করবে **"Wrong"** এবং আবার ইনপুট নেবে।

---

## 📥 Input:
একাধিক ইনপুট দেওয়া যেতে পারে (একবারে এক সংখ্যা করে)।  
প্রোগ্রামটি তখনই থামবে যখন ইনপুট **১৯৯৯** হবে।

**Example Input:**
```mermaid
2200
1020
1999
```
---
## 📤 Output:
```mermaid
Wrong
Wrong
Correct
```
---

## 🧠 Logic:
- প্রোগ্রামটি একটি **infinite loop (`while(1)`)** ব্যবহার করে।
- প্রতিবার ইউজার একটি সংখ্যা ইনপুট দেয়।
- যদি সংখ্যাটি `1999` হয় → `"Correct"` প্রিন্ট করে `break` দিয়ে লুপ থামায়।
- অন্যথায় `"Wrong"` প্রিন্ট করে পরের ইনপুটের জন্য অপেক্ষা করে।

---

## 💡 Code:
```cpp
#include <iostream>
using namespace std;
int main() {
    int n = 1999, j;
    while (1) {
        cin >> j;
        if (j == n) {
            cout << "Correct";
            break;
        } else {
            cout << "Wrong" << endl;
        }
    }
    return 0;
}
```

---
## ✅ Tags:
Loops, While Loop, Infinite Loop, Conditionals, C++, Beginner

---

## 🔚 Final Notes:
এই প্রবলেমটি infinite loop, break statement, এবং conditional checking শেখার জন্য দারুণ উদাহরণ।
এটি ব্যবহারকারী ইনপুট যাচাই (user authentication logic) বোঝার একটি প্রাথমিক ধাপও।